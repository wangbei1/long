denoising_step_list:
- 1000
- 750
- 500
- 250
warp_denoising_step: true # need to remove - 0 in denoising_step_list if warp_denoising_step is true
num_frame_per_block: 3
model_name: Wan2.1-T2V-1.3B
model_kwargs:
  local_attn_size: 12
  timestep_shift: 5.0
  sink_size: 3


# inference
data_path: /home/zdmaogroup/wubin/reward-forcing-claude-add-experiment-runner-script-PPlnc/reward-forcing-claude-spatial-reward-forcing-L5245/LongLive-main/docs/MovieGenVideoBench.txt
output_folder: videos/long_180s
inference_iter: 127          # run prompts 0..127 (first 128 lines of the file)
num_output_frames: 720       # 720 latent frames ≈ 2880 pixel frames @16fps = 180s
use_ema: false
seed: 0
num_samples: 1
save_with_index: true
global_sink: true
context_noise: 0

# 只用短片训练得到的主干, 不加载 LoRA
generator_ckpt: longlive_models/models/longlive_base.pt
# lora_ckpt / adapter 均未设置 -> inference.py 不会给 generator 套 LoRA
